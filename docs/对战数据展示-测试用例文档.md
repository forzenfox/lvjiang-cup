# 对战数据展示功能 - 完整测试用例文档

> **文档版本**: v1.0  
> **创建日期**: 2026-04-20  
> **编写依据**:  
> - PRD文档: `docs/PRD-对战数据展示.md`  
> - 导入模板设计文档: `docs/对战数据导入模板设计方案.md`  
> **适用范围**: 前端 E2E 测试编写指导  

---

## 一、Excel 文件导入功能测试

### 1.1 文件格式校验

| 用例编号 | MD-IMPORT-001 |
|---------|--------------|
| **用例标题** | 上传正确格式的 .xlsx 文件 |
| **模块** | Excel 文件导入 - 格式校验 |
| **优先级** | P0 |
| **前置条件** | 1. 管理员已登录管理后台<br>2. 已进入"对战数据管理"页面<br>3. 列表中有一条已结束的对战记录（如 BO3 赛制）<br>4. 已准备好一份符合模板规范的 .xlsx 文件 |
| **测试步骤** | 1. 点击该对战记录的"对战数据导入"按钮<br>2. 在弹出的上传对话框中，选择准备好的 .xlsx 文件<br>3. 点击"确认上传"按钮 |
| **预期结果** | 1. 文件上传成功，显示"上传成功"提示<br>2. 数据解析后自动关联到当前对战<br>3. 页面刷新后该记录显示"编辑"按钮 |

| 用例编号 | MD-IMPORT-002 |
|---------|--------------|
| **用例标题** | 上传非 .xlsx 格式文件（.xls） |
| **模块** | Excel 文件导入 - 格式校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录，进入对战数据管理页面，选择一条已结束对战 |
| **测试步骤** | 1. 点击"对战数据导入"按钮<br>2. 选择一份 .xls 格式（Excel 97-2003）的文件<br>3. 点击"确认上传" |
| **预期结果** | 1. 系统拒绝上传，提示"仅支持 .xlsx 格式文件"<br>2. 文件未被解析 |

| 用例编号 | MD-IMPORT-003 |
|---------|--------------|
| **用例标题** | 上传非 Excel 文件（.csv / .pdf / .png） |
| **模块** | Excel 文件导入 - 格式校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录，进入对战数据管理页面 |
| **测试步骤** | 1. 点击"对战数据导入"按钮<br>2. 分别选择 .csv、.pdf、.png 格式的文件尝试上传 |
| **预期结果** | 1. 每种格式均被拒绝，提示"仅支持 .xlsx 格式文件"<br>2. 文件选择器应默认过滤非 .xlsx 文件 |

| 用例编号 | MD-IMPORT-004 |
|---------|--------------|
| **用例标题** | 上传空文件（0 字节） |
| **模块** | Excel 文件导入 - 格式校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录，进入对战数据管理页面 |
| **测试步骤** | 1. 点击"对战数据导入"按钮<br>2. 选择一个 0 字节的 .xlsx 空文件<br>3. 点击"确认上传" |
| **预期结果** | 1. 系统提示"文件为空或格式不正确"<br>2. 上传失败 |

| 用例编号 | MD-IMPORT-005 |
|---------|--------------|
| **用例标题** | 上传损坏的 .xlsx 文件 |
| **模块** | Excel 文件导入 - 格式校验 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录，进入对战数据管理页面 |
| **测试步骤** | 1. 点击"对战数据导入"按钮<br>2. 选择一个内容被破坏的 .xlsx 文件（如用文本编辑器修改后保存）<br>3. 点击"确认上传" |
| **预期结果** | 1. 系统提示"文件损坏，无法解析"<br>2. 上传失败 |

### 1.2 文件结构校验

| 用例编号 | MD-STRUCT-001 |
|---------|--------------|
| **用例标题** | 上传标准 16 行结构的模板文件 |
| **模块** | Excel 文件导入 - 结构校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，选择一条已结束对战 |
| **测试步骤** | 1. 准备一份完整的 16 行模板文件（MatchInfo 2行 + TeamStats 3行 + PlayerStats 11行）<br>2. 上传该文件 |
| **预期结果** | 1. 解析成功，所有数据块正确识别<br>2. 数据正确入库 |

| 用例编号 | MD-STRUCT-002 |
|---------|--------------|
| **用例标题** | 上传行数不足的文件（少于 16 行） |
| **模块** | Excel 文件导入 - 结构校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，选择一条已结束对战 |
| **测试步骤** | 1. 准备一份只有 10 行的 .xlsx 文件（缺少 PlayerStats 数据行）<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"文件结构不完整，缺少选手数据（应为 16 行，实际 N 行）"<br>2. 上传失败，高亮显示缺失区域 |

| 用例编号 | MD-STRUCT-003 |
|---------|--------------|
| **用例标题** | 上传行数超出预期的文件（多于 16 行） |
| **模块** | Excel 文件导入 - 结构校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录，选择一条已结束对战 |
| **测试步骤** | 1. 准备一份有 20 行的 .xlsx 文件（PlayerStats 区域额外添加了 4 行）<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"文件行数超出预期，标准文件应为 16 行"<br>2. 或系统忽略多余行，仅解析前 16 行（需确认需求） |

| 用例编号 | MD-STRUCT-004 |
|---------|--------------|
| **用例标题** | 缺少 MatchInfo 表头行（第 1 行） |
| **模块** | Excel 文件导入 - 结构校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份第 1 行空白的文件（MatchInfo 表头缺失）<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"文件结构错误：缺少 MatchInfo 表头行"<br>2. 上传失败 |

| 用例编号 | MD-STRUCT-005 |
|---------|--------------|
| **用例标题** | 缺少 TeamStats 表头行（第 3 行） |
| **模块** | Excel 文件导入 - 结构校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份第 3 行空白的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"文件结构错误：缺少 TeamStats 表头行"<br>2. 上传失败 |

| 用例编号 | MD-STRUCT-006 |
|---------|--------------|
| **用例标题** | 缺少 PlayerStats 表头行（第 6 行） |
| **模块** | Excel 文件导入 - 结构校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份第 6 行空白的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"文件结构错误：缺少 PlayerStats 表头行"<br>2. 上传失败 |

| 用例编号 | MD-STRUCT-007 |
|---------|--------------|
| **用例标题** | TeamStats 数据行不足（只有红方，缺少蓝方） |
| **模块** | Excel 文件导入 - 结构校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份第 5 行空白的文件（只有红方战队数据）<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"战队数据不完整，需要红蓝双方各 1 条数据"<br>2. 上传失败 |

| 用例编号 | MD-STRUCT-008 |
|---------|--------------|
| **用例标题** | PlayerStats 数据行不足（只有红方 5 人，缺少蓝方） |
| **模块** | Excel 文件导入 - 结构校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份只有 11 行的文件（第 12-16 行空白）<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"选手数据不完整，需要双方各 5 名选手（共 10 条）"<br>2. 上传失败 |

### 1.3 文件内容数据校验

#### 1.3.1 MatchInfo 数据校验

| 用例编号 | MD-MATCH-001 |
|---------|--------------|
| **用例标题** | MatchInfo - 红方战队名为空 |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份第 2 行"红方战队名"为空的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"红方战队名不能为空"<br>2. 上传失败 |

| 用例编号 | MD-MATCH-002 |
|---------|--------------|
| **用例标题** | MatchInfo - 红方战队名不存在于数据库 |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，数据库中存在战队 BLG 和 WBG |
| **测试步骤** | 1. 准备一份红方战队名为"T1"（不存在）的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"战队 'T1' 不存在或名称重复"<br>2. 上传失败 |

| 用例编号 | MD-MATCH-003 |
|---------|--------------|
| **用例标题** | MatchInfo - 蓝方战队名为空 |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份第 2 行"蓝方战队名"为空的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"蓝方战队名不能为空"<br>2. 上传失败 |

| 用例编号 | MD-MATCH-004 |
|---------|--------------|
| **用例标题** | MatchInfo - 红蓝方战队名相同 |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份红方和蓝方战队名均为"BLG"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"红蓝方不能为同一战队"<br>2. 上传失败 |

| 用例编号 | MD-MATCH-005 |
|---------|--------------|
| **用例标题** | MatchInfo - 局数超出赛制限制（BO3 上传第 4 局） |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，选择的对战为 BO3 赛制 |
| **测试步骤** | 1. 准备一份局数为 4 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"局数超出赛制限制（BO3 最多 3 局）"<br>2. 返回错误码 40002<br>3. 上传失败 |

| 用例编号 | MD-MATCH-006 |
|---------|--------------|
| **用例标题** | MatchInfo - 局数为 0 或负数 |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 分别准备局数为 0 和 -1 的文件<br>2. 上传并验证 |
| **预期结果** | 1. 系统提示"局数必须在 1-5 之间"<br>2. 上传失败 |

| 用例编号 | MD-MATCH-007 |
|---------|--------------|
| **用例标题** | MatchInfo - 局数为非整数（如 1.5） |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份局数为 1.5 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"局数必须为整数"<br>2. 上传失败 |

| 用例编号 | MD-MATCH-008 |
|---------|--------------|
| **用例标题** | MatchInfo - 比赛时间格式错误 |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份比赛时间为"2026/04/16"或"14:00"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"比赛时间格式错误，应为 YYYY-MM-DD HH:mm"<br>2. 上传失败 |

| 用例编号 | MD-MATCH-009 |
|---------|--------------|
| **用例标题** | MatchInfo - 比赛时间为空 |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份比赛时间为空的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"比赛时间不能为空"<br>2. 上传失败 |

| 用例编号 | MD-MATCH-010 |
|---------|--------------|
| **用例标题** | MatchInfo - 游戏时长格式错误 |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 分别准备游戏时长为"32"、"32.45"、"32分45秒"的文件<br>2. 上传并验证 |
| **预期结果** | 1. 系统提示"游戏时长格式错误，应为 MM:SS"<br>2. 上传失败 |

| 用例编号 | MD-MATCH-011 |
|---------|--------------|
| **用例标题** | MatchInfo - 游戏时长为 00:00 |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份游戏时长为"00:00"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"游戏时长不能为 0"<br>2. 上传失败 |

| 用例编号 | MD-MATCH-012 |
|---------|--------------|
| **用例标题** | MatchInfo - 游戏时长为极端值（如 99:59） |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份游戏时长为"99:59"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统允许上传（或根据业务规则提示异常时长）<br>2. 需确认业务上是否允许超过 60 分钟的对局 |

| 用例编号 | MD-MATCH-013 |
|---------|--------------|
| **用例标题** | MatchInfo - 获胜方为空 |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份获胜方为空的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"获胜方不能为空"<br>2. 上传失败 |

| 用例编号 | MD-MATCH-014 |
|---------|--------------|
| **用例标题** | MatchInfo - 获胜方值无效（非 red/blue） |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 分别准备获胜方为"red方"、"blue方"、"A队"、"true"的文件<br>2. 上传并验证 |
| **预期结果** | 1. 系统提示"获胜方 '{值}' 无效，应为 red 或 blue"<br>2. 上传失败 |

| 用例编号 | MD-MATCH-015 |
|---------|--------------|
| **用例标题** | MatchInfo - 获胜方与红蓝方战队不匹配 |
| **模块** | Excel 文件导入 - MatchInfo 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份红方为 BLG、蓝方为 WBG、但获胜方为"green"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"获胜方 '{值}' 无效，应为 red 或 blue"<br>2. 上传失败 |

#### 1.3.2 TeamStats 数据校验

| 用例编号 | MD-TEAM-001 |
|---------|--------------|
| **用例标题** | TeamStats - 阵营值无效 |
| **模块** | Excel 文件导入 - TeamStats 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份红方阵营值为"red方"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"阵营 '{值}' 无效，应为 red 或 blue"<br>2. 上传失败 |

| 用例编号 | MD-TEAM-002 |
|---------|--------------|
| **用例标题** | TeamStats - 战队名与 MatchInfo 中不一致 |
| **模块** | Excel 文件导入 - TeamStats 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份 MatchInfo 中红方为 BLG、但 TeamStats 中红方战队名为"EDG"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"TeamStats 中战队名与 MatchInfo 不一致"<br>2. 上传失败 |

| 用例编号 | MD-TEAM-003 |
|---------|--------------|
| **用例标题** | TeamStats - 总击杀为负数 |
| **模块** | Excel 文件导入 - TeamStats 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份红方总击杀为 -5 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"击杀数不能为负数或超过 100"<br>2. 上传失败 |

| 用例编号 | MD-TEAM-004 |
|---------|--------------|
| **用例标题** | TeamStats - 总击杀超过上限（>100） |
| **模块** | Excel 文件导入 - TeamStats 校验 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份红方总击杀为 101 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"击杀数不能为负数或超过 100"<br>2. 上传失败 |

| 用例编号 | MD-TEAM-005 |
|---------|--------------|
| **用例标题** | TeamStats - 总经济为负数 |
| **模块** | Excel 文件导入 - TeamStats 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份红方总经济为 -1000 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"经济不能为负数"<br>2. 上传失败 |

| 用例编号 | MD-TEAM-006 |
|---------|--------------|
| **用例标题** | TeamStats - 总经济超过上限（>100000） |
| **模块** | Excel 文件导入 - TeamStats 校验 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份红方总经济为 100001 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"经济超过上限（最大 100000）"<br>2. 上传失败 |

| 用例编号 | MD-TEAM-007 |
|---------|--------------|
| **用例标题** | TeamStats - 一血值无效（非 yes/no） |
| **模块** | Excel 文件导入 - TeamStats 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份红方一血为"YES"或"1"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"一血值无效，应为 yes 或 no"<br>2. 上传失败 |

| 用例编号 | MD-TEAM-008 |
|---------|--------------|
| **用例标题** | TeamStats - 双方均获得一血（数据矛盾） |
| **模块** | Excel 文件导入 - TeamStats 校验 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份红方和蓝方一血均为"yes"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"只能有一方获得一血"<br>2. 或系统接受但提示数据异常（需确认业务规则） |

| 用例编号 | MD-TEAM-009 |
|---------|--------------|
| **用例标题** | TeamStats - 推塔数/控龙数/控Baron数为负数 |
| **模块** | Excel 文件导入 - TeamStats 校验 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份推塔数为 -1 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"推塔数不能为负数"<br>2. 上传失败 |

#### 1.3.3 PlayerStats 数据校验

| 用例编号 | MD-PLAYER-001 |
|---------|--------------|
| **用例标题** | PlayerStats - 选手昵称为空 |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，数据库中存在选手 Bin |
| **测试步骤** | 1. 准备一份某选手昵称为空的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"选手昵称不能为空"<br>2. 上传失败 |

| 用例编号 | MD-PLAYER-002 |
|---------|--------------|
| **用例标题** | PlayerStats - 选手昵称不存在（模糊匹配失败） |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，数据库中存在选手 Bin |
| **测试步骤** | 1. 准备一份选手昵称为"Bin123"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"选手 'Bin123' 不存在"<br>2. 上传失败 |

| 用例编号 | MD-PLAYER-003 |
|---------|--------------|
| **用例标题** | PlayerStats - 选手昵称前后带空格（模糊匹配应成功） |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录，数据库中存在选手 Bin |
| **测试步骤** | 1. 准备一份选手昵称为"  Bin  "（前后有空格）的文件<br>2. 上传该文件 |
| **预期结果** | 1. 模糊匹配成功，识别为选手 Bin<br>2. 上传成功 |

| 用例编号 | MD-PLAYER-004 |
|---------|--------------|
| **用例标题** | PlayerStats - 选手昵称大小写不一致（模糊匹配应成功） |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录，数据库中存在选手 Bin |
| **测试步骤** | 1. 准备一份选手昵称为"bin"或"BIN"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 模糊匹配成功，忽略大小写<br>2. 上传成功 |

| 用例编号 | MD-PLAYER-005 |
|---------|--------------|
| **用例标题** | PlayerStats - 选手昵称匹配到多个结果 |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录，数据库中存在两个昵称相似的选手 |
| **测试步骤** | 1. 准备一份选手昵称能匹配到多个选手的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"选手 '{昵称}' 匹配到多个结果，请提供更精确的名称"<br>2. 上传失败 |

| 用例编号 | MD-PLAYER-006 |
|---------|--------------|
| **用例标题** | PlayerStats - 英雄名为空 |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份某选手英雄名为空的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"英雄名不能为空"<br>2. 上传失败 |

| 用例编号 | MD-PLAYER-007 |
|---------|--------------|
| **用例标题** | PlayerStats - 英雄名不在英雄字典中 |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份英雄名为"未知英雄"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"英雄名称 '未知英雄' 不存在"<br>2. 上传失败 |

| 用例编号 | MD-PLAYER-008 |
|---------|--------------|
| **用例标题** | PlayerStats - 位置值无效 |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份位置为"TOP1"或"上单"或"top"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"位置 '{值}' 无效，应为 TOP/JUNGLE/MID/ADC/SUPPORT"<br>2. 上传失败 |

| 用例编号 | MD-PLAYER-009 |
|---------|--------------|
| **用例标题** | PlayerStats - 击杀数为负数 |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份击杀数为 -1 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"击杀数不能为负数或超过 100"<br>2. 上传失败 |

| 用例编号 | MD-PLAYER-010 |
|---------|--------------|
| **用例标题** | PlayerStats - 补刀数超过上限（>1000） |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份补刀数为 1001 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"补刀数不能为负数或超过 1000"<br>2. 上传失败 |

| 用例编号 | MD-PLAYER-011 |
|---------|--------------|
| **用例标题** | PlayerStats - 等级超出范围（<1 或 >18） |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 分别准备等级为 0 和 19 的文件<br>2. 上传并验证 |
| **预期结果** | 1. 系统提示"等级必须在 1-18 之间"<br>2. 上传失败 |

| 用例编号 | MD-PLAYER-012 |
|---------|--------------|
| **用例标题** | PlayerStats - 非必填字段为空（插眼数、排眼数、是否MVP） |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份插眼数、排眼数、是否MVP均为空的文件<br>2. 上传该文件 |
| **预期结果** | 1. 上传成功，非必填字段以默认值或 null 入库<br>2. 页面展示时对应字段显示"-" |

| 用例编号 | MD-PLAYER-013 |
|---------|--------------|
| **用例标题** | PlayerStats - 红方选手未按位置顺序排列 |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份红方选手顺序为 ADC→SUPPORT→TOP→JUNGLE→MID 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统应自动按 TOP→JUNGLE→MID→ADC→SUPPORT 排序<br>2. 或提示"选手数据需按位置排序"（需确认需求） |

| 用例编号 | MD-PLAYER-014 |
|---------|--------------|
| **用例标题** | PlayerStats - 同一选手出现两次 |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份选手 Bin 在红方出现两次的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"选手 'Bin' 在文件中重复出现"<br>2. 上传失败 |

| 用例编号 | MD-PLAYER-015 |
|---------|--------------|
| **用例标题** | PlayerStats - 选手阵营与所属战队不匹配 |
| **模块** | Excel 文件导入 - PlayerStats 校验 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录，选手 Bin 属于 BLG 战队 |
| **测试步骤** | 1. 准备一份选手 Bin 出现在蓝方（WBG）阵营的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"选手 'Bin' 不属于蓝方战队 'WBG'"<br>2. 上传失败 |

---

## 二、数据解析功能测试

### 2.1 三表数据解析

| 用例编号 | MD-PARSE-001 |
|---------|--------------|
| **用例标题** | 正确解析 MatchInfo 全部 6 个字段 |
| **模块** | 数据解析 - MatchInfo |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，上传一份完整有效的文件 |
| **测试步骤** | 1. 上传标准文件<br>2. 查看解析后的 MatchInfo 数据<br>3. 逐一核对 6 个字段值 |
| **预期结果** | 1. 红方战队名、蓝方战队名、局数、比赛时间、游戏时长、获胜方均正确解析<br>2. 数据类型正确（局数为整数、时间为 datetime 等） |

| 用例编号 | MD-PARSE-002 |
|---------|--------------|
| **用例标题** | 正确解析 TeamStats 全部字段（红蓝双方各 9 个字段） |
| **模块** | 数据解析 - TeamStats |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，上传一份完整有效的文件 |
| **测试步骤** | 1. 上传标准文件<br>2. 查看解析后的 TeamStats 数据<br>3. 逐一核对红蓝双方各 9 个字段值 |
| **预期结果** | 1. 红方和蓝方的阵营、战队名、总击杀、总死亡、总助攻、总经济、推塔数、控龙数、控Baron数、一血均正确解析 |

| 用例编号 | MD-PARSE-003 |
|---------|--------------|
| **用例标题** | 正确解析 PlayerStats 全部 10 名选手数据（每方 5 人） |
| **模块** | 数据解析 - PlayerStats |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，上传一份完整有效的文件 |
| **测试步骤** | 1. 上传标准文件<br>2. 查看解析后的 PlayerStats 数据<br>3. 逐一核对 10 名选手的全部字段 |
| **预期结果** | 1. 红方 5 人和蓝方 5 人数据均正确解析<br>2. 每个选手的阵营、位置、昵称、英雄名、KDA、补刀、经济、伤害、承伤、等级、视野得分、插眼数、排眼数、是否MVP均正确 |

| 用例编号 | MD-PARSE-004 |
|---------|--------------|
| **用例标题** | 解析含特殊字符的数据（选手昵称含特殊字符） |
| **模块** | 数据解析 - 特殊字符处理 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份选手昵称为"小明'王"（含单引号）的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统正确处理特殊字符，不报 SQL 注入错误<br>2. 数据正确入库 |

| 用例编号 | MD-PARSE-005 |
|---------|--------------|
| **用例标题** | 解析含超长文本的数据（英雄名超长） |
| **模块** | 数据解析 - 边界值 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份英雄名超过 50 个字符的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"字段长度超出限制"<br>2. 或自动截断并提示（需确认需求） |

### 2.2 数据类型转换

| 用例编号 | MD-TYPE-001 |
|---------|--------------|
| **用例标题** | 数值字段被填写为文本格式的容错处理 |
| **模块** | 数据解析 - 类型转换 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份击杀数为文本"8"（而非数字 8）的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统自动将文本数字转换为数值类型<br>2. 上传成功 |

| 用例编号 | MD-TYPE-002 |
|---------|--------------|
| **用例标题** | 数值字段被填写为非数字文本 |
| **模块** | 数据解析 - 类型转换 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份击杀数为"八"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"击杀数必须为数字"<br>2. 上传失败 |

| 用例编号 | MD-TYPE-003 |
|---------|--------------|
| **用例标题** | 数值字段含小数点（如击杀数 8.5） |
| **模块** | 数据解析 - 类型转换 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份击杀数为 8.5 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"击杀数必须为整数"<br>2. 上传失败 |

---

## 三、数据关联功能测试

### 3.1 战队名关联

| 用例编号 | MD-RELATE-001 |
|---------|--------------|
| **用例标题** | 战队名精确匹配成功 |
| **模块** | 数据关联 - 战队名 |
| **优先级** | P0 |
| **前置条件** | 数据库中存在战队 BLG（名称唯一） |
| **测试步骤** | 1. 准备一份红方战队名为"BLG"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 精确匹配到战队 BLG<br>2. 关联成功，数据正确入库 |

| 用例编号 | MD-RELATE-002 |
|---------|--------------|
| **用例标题** | 战队名带前后空格（应匹配失败，精确匹配不忽略空格） |
| **模块** | 数据关联 - 战队名 |
| **优先级** | P1 |
| **前置条件** | 数据库中存在战队 BLG |
| **测试步骤** | 1. 准备一份红方战队名为" BLG "的文件<br>2. 上传该文件 |
| **预期结果** | 1. 精确匹配失败，提示"战队 ' BLG ' 不存在或名称重复"<br>2. 上传失败 |

| 用例编号 | MD-RELATE-003 |
|---------|--------------|
| **用例标题** | 战队名大小写不一致（精确匹配应区分大小写） |
| **模块** | 数据关联 - 战队名 |
| **优先级** | P1 |
| **前置条件** | 数据库中存在战队 BLG |
| **测试步骤** | 1. 准备一份红方战队名为"blg"或"Blg"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 精确匹配失败，提示"战队 'blg' 不存在或名称重复"<br>2. 上传失败 |

### 3.2 选手昵称关联

| 用例编号 | MD-RELATE-004 |
|---------|--------------|
| **用例标题** | 选手昵称精确匹配成功 |
| **模块** | 数据关联 - 选手昵称 |
| **优先级** | P0 |
| **前置条件** | 数据库中存在选手，昵称为"Bin" |
| **测试步骤** | 1. 准备一份选手昵称为"Bin"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 精确匹配成功<br>2. 关联到正确选手 |

| 用例编号 | MD-RELATE-005 |
|---------|--------------|
| **用例标题** | 选手昵称模糊匹配成功（忽略大小写） |
| **模块** | 数据关联 - 选手昵称 |
| **优先级** | P1 |
| **前置条件** | 数据库中存在选手，昵称为"Bin" |
| **测试步骤** | 1. 准备一份选手昵称为"bin"或"BIN"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 模糊匹配成功，忽略大小写<br>2. 关联到正确选手 |

| 用例编号 | MD-RELATE-006 |
|---------|--------------|
| **用例标题** | 选手昵称模糊匹配成功（忽略前后空格） |
| **模块** | 数据关联 - 选手昵称 |
| **优先级** | P1 |
| **前置条件** | 数据库中存在选手，昵称为"Bin" |
| **测试步骤** | 1. 准备一份选手昵称为"  Bin  "的文件<br>2. 上传该文件 |
| **预期结果** | 1. 模糊匹配成功，忽略前后空格<br>2. 关联到正确选手 |

### 3.3 英雄名关联

| 用例编号 | MD-RELATE-007 |
|---------|--------------|
| **用例标题** | 英雄名精确匹配成功 |
| **模块** | 数据关联 - 英雄名 |
| **优先级** | P0 |
| **前置条件** | 英雄字典中存在"亚索" |
| **测试步骤** | 1. 准备一份英雄名为"亚索"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 精确匹配成功<br>2. 关联到正确英雄 |

| 用例编号 | MD-RELATE-008 |
|---------|--------------|
| **用例标题** | 英雄名匹配失败 |
| **模块** | 数据关联 - 英雄名 |
| **优先级** | P0 |
| **前置条件** | 英雄字典中不存在"暗影猎手" |
| **测试步骤** | 1. 准备一份英雄名为"暗影猎手"的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示"英雄名称 '暗影猎手' 不存在"<br>2. 上传失败 |

---

## 四、管理页面功能测试

### 4.1 列表展示

| 用例编号 | MD-MGMT-001 |
|---------|--------------|
| **用例标题** | 仅显示已结束状态的对战记录 |
| **模块** | 管理页面 - 列表展示 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，进入"对战数据"管理页面 |
| **测试步骤** | 1. 进入管理页面<br>2. 查看列表中对战记录的状态 |
| **预期结果** | 1. 列表中仅显示"已结束"状态的对战<br>2. "进行中"和"未开始"的对战不显示 |

| 用例编号 | MD-MGMT-002 |
|---------|--------------|
| **用例标题** | 列表筛选功能 - 按赛事名称筛选 |
| **模块** | 管理页面 - 筛选 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录，管理页面有多个赛事的对战记录 |
| **测试步骤** | 1. 在"赛事名称"下拉框中选择一个赛事<br>2. 点击"搜索"按钮 |
| **预期结果** | 1. 列表仅显示所选赛事的对战记录<br>2. 列表数量正确 |

| 用例编号 | MD-MGMT-003 |
|---------|--------------|
| **用例标题** | 列表筛选功能 - 按日期范围筛选 |
| **模块** | 管理页面 - 筛选 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 选择日期范围（如 2026-04-15 至 2026-04-16）<br>2. 点击"搜索" |
| **预期结果** | 1. 列表仅显示该日期范围内的对战记录 |

| 用例编号 | MD-MGMT-004 |
|---------|--------------|
| **用例标题** | 列表分页功能 |
| **模块** | 管理页面 - 分页 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录，对战记录超过 10 条 |
| **测试步骤** | 1. 查看列表默认显示第 1 页<br>2. 点击第 2 页<br>3. 点击"下一页"按钮 |
| **预期结果** | 1. 每页显示 10 条记录<br>2. 翻页后数据正确切换<br>3. 页码高亮正确 |

| 用例编号 | MD-MGMT-005 |
|---------|--------------|
| **用例标题** | 已导入数据的记录显示"编辑"按钮 |
| **模块** | 管理页面 - 按钮显示 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，某条对战已导入数据 |
| **测试步骤** | 1. 查看该记录的操作列 |
| **预期结果** | 1. 该记录显示"追加导入"和"编辑"两个按钮 |

| 用例编号 | MD-MGMT-006 |
|---------|--------------|
| **用例标题** | 未导入数据的记录不显示"编辑"按钮 |
| **模块** | 管理页面 - 按钮显示 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，某条对战未导入数据 |
| **测试步骤** | 1. 查看该记录的操作列 |
| **预期结果** | 1. 该记录仅显示"对战数据导入"按钮，不显示"编辑"按钮 |

### 4.2 追加导入

| 用例编号 | MD-MGMT-007 |
|---------|--------------|
| **用例标题** | 对已有数据的对战进行追加导入 |
| **模块** | 管理页面 - 追加导入 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，某 BO3 对战已导入第 1 局数据 |
| **测试步骤** | 1. 点击该记录的"追加导入"按钮<br>2. 上传第 2 局数据文件（局数=2） |
| **预期结果** | 1. 追加导入成功<br>2. 对战详情页可查看第 1 局和第 2 局数据 |

| 用例编号 | MD-MGMT-008 |
|---------|--------------|
| **用例标题** | 追加导入相同局数数据（覆盖场景） |
| **模块** | 管理页面 - 追加导入 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录，某 BO3 对战已导入第 1 局数据 |
| **测试步骤** | 1. 点击"追加导入"按钮<br>2. 再次上传第 1 局数据文件（局数=1） |
| **预期结果** | 1. 系统提示"该对局数据已存在，是否覆盖？"<br>2. 确认覆盖后更新数据<br>3. 取消覆盖则保留原数据 |

---

## 五、数据展示功能测试

### 5.1 对战数据按钮显示控制

| 用例编号 | MD-DISPLAY-001 |
|---------|--------------|
| **用例标题** | 未上传数据时不显示"对战数据"按钮 |
| **模块** | 数据展示 - 按钮控制 |
| **优先级** | P0 |
| **前置条件** | 用户已登录（普通用户），进入某场对战的详情页面 |
| **测试步骤** | 1. 确认该对战未上传任何数据<br>2. 查看操作区域 |
| **预期结果** | 1. 不显示"对战数据"按钮<br>2. 不显示任何占位符 |

| 用例编号 | MD-DISPLAY-002 |
|---------|--------------|
| **用例标题** | 上传数据后显示"对战数据"按钮 |
| **模块** | 数据展示 - 按钮控制 |
| **优先级** | P0 |
| **前置条件** | 普通用户，某场对战已上传至少一条数据 |
| **测试步骤** | 1. 进入该对战的详情页面<br>2. 查看操作区域 |
| **预期结果** | 1. 显示"对战数据"按钮<br>2. 按钮样式正确（主色调，图标 + 文字） |

| 用例编号 | MD-DISPLAY-003 |
|---------|--------------|
| **用例标题** | 点击"对战数据"按钮打开新标签页 |
| **模块** | 数据展示 - 按钮交互 |
| **优先级** | P0 |
| **前置条件** | 普通用户，某场对战已上传数据 |
| **测试步骤** | 1. 点击"对战数据"按钮 |
| **预期结果** | 1. 在新标签页中打开对战数据详情页面<br>2. 原标签页保持不变 |

| 用例编号 | MD-DISPLAY-004 |
|---------|--------------|
| **用例标题** | 鼠标悬停按钮时显示手型指针和放大效果 |
| **模块** | 数据展示 - 按钮交互 |
| **优先级** | P2 |
| **前置条件** | 普通用户，进入对战详情页面 |
| **测试步骤** | 1. 将鼠标悬停在"对战数据"按钮上 |
| **预期结果** | 1. 鼠标变为手型指针<br>2. 按钮有轻微放大效果 |

### 5.2 对战数据页面展示（只读模式）

| 用例编号 | MD-DISPLAY-005 |
|---------|--------------|
| **用例标题** | 对战基本信息展示正确 |
| **模块** | 数据展示 - 基本信息 |
| **优先级** | P0 |
| **前置条件** | 用户已对战数据详情页面，数据已加载完成 |
| **测试步骤** | 1. 查看基本信息区域 |
| **预期结果** | 1. 正确显示"战队 A vs 战队 B"<br>2. 正确显示赛制和局数（如"BO3 - 第 1 局"）<br>3. 正确显示比赛时间（如"2026-04-16 14:00"）<br>4. 正确显示游戏时长（如"32:45"）<br>5. 正确显示获胜方 |

| 用例编号 | MD-DISPLAY-006 |
|---------|--------------|
| **用例标题** | LPL 对称式布局展示正确（红蓝方对称） |
| **模块** | 数据展示 - 布局 |
| **优先级** | P0 |
| **前置条件** | 用户已进入对战数据详情页面 |
| **测试步骤** | 1. 查看选手数据展示区域 |
| **预期结果** | 1. 左侧为红色方（A 队）选手数据<br>2. 右侧为蓝色方（B 队）选手数据<br>3. 两侧对称排列<br>4. 中间区域显示关键数据对比 |

| 用例编号 | MD-DISPLAY-007 |
|---------|--------------|
| **用例标题** | 选手数据按位置正确排序 |
| **模块** | 数据展示 - 排序 |
| **优先级** | P0 |
| **前置条件** | 用户已进入对战数据详情页面 |
| **测试步骤** | 1. 查看每队选手排列顺序 |
| **预期结果** | 1. 选手按 TOP → JUNGLE → MID → ADC → SUPPORT 顺序排列<br>2. 每个选手的信息完整（昵称、英雄、KDA、经济、补刀等） |

| 用例编号 | MD-DISPLAY-008 |
|---------|--------------|
| **用例标题** | 鼠标悬停选手行时高亮显示 |
| **模块** | 数据展示 - 交互 |
| **优先级** | P2 |
| **前置条件** | 用户已进入对战数据详情页面 |
| **测试步骤** | 1. 将鼠标悬停在某选手数据行上 |
| **预期结果** | 1. 该行高亮显示（背景色变化）<br>2. 鼠标离开后恢复原样式 |

| 用例编号 | MD-DISPLAY-009 |
|---------|--------------|
| **用例标题** | 数据加载中显示骨架屏 |
| **模块** | 数据展示 - 加载状态 |
| **优先级** | P1 |
| **前置条件** | 用户进入对战数据详情页面 |
| **测试步骤** | 1. 在网络较慢的情况下进入页面<br>2. 观察加载过程 |
| **预期结果** | 1. 数据加载过程中显示骨架屏<br>2. 基本信息区域为灰色占位块<br>3. 选手数据区域为灰色行占位 |

| 用例编号 | MD-DISPLAY-010 |
|---------|--------------|
| **用例标题** | 加载失败显示错误提示和重试按钮 |
| **模块** | 数据展示 - 错误状态 |
| **优先级** | P1 |
| **前置条件** | 用户进入对战数据详情页面 |
| **测试步骤** | 1. 断开网络连接或模拟接口返回错误<br>2. 观察页面状态 |
| **预期结果** | 1. 显示"数据加载失败，请刷新重试"<br>2. 显示刷新按钮<br>3. 点击刷新按钮可重新加载 |

| 用例编号 | MD-DISPLAY-011 |
|---------|--------------|
| **用例标题** | 数据为空显示空状态提示 |
| **模块** | 数据展示 - 空状态 |
| **优先级** | P1 |
| **前置条件** | 用户访问一个不存在数据的对局 URL |
| **测试步骤** | 1. 访问 `/match/{id}/games?game=999`（不存在的局数） |
| **预期结果** | 1. 显示"该对局数据不存在"<br>2. 自动跳转到第一局或提供返回按钮 |

### 5.3 战队筛选和位置筛选

| 用例编号 | MD-FILTER-001 |
|---------|--------------|
| **用例标题** | 战队筛选 - 显示全部战队 |
| **模块** | 数据展示 - 筛选 |
| **优先级** | P1 |
| **前置条件** | 用户已进入对战数据详情页面 |
| **测试步骤** | 1. 在战队筛选下拉框中选择"全部"<br>2. 观察选手数据展示 |
| **预期结果** | 1. 显示双方全部 10 名选手的数据<br>2. 对称布局正常 |

| 用例编号 | MD-FILTER-002 |
|---------|--------------|
| **用例标题** | 战队筛选 - 仅显示战队 A |
| **模块** | 数据展示 - 筛选 |
| **优先级** | P1 |
| **前置条件** | 用户已进入对战数据详情页面 |
| **测试步骤** | 1. 在战队筛选下拉框中选择"战队 A" |
| **预期结果** | 1. 仅显示战队 A 的 5 名选手数据<br>2. 选手按位置顺序排列 |

| 用例编号 | MD-FILTER-003 |
|---------|--------------|
| **用例标题** | 位置筛选 - 仅显示 TOP 选手 |
| **模块** | 数据展示 - 筛选 |
| **优先级** | P1 |
| **前置条件** | 用户已进入对战数据详情页面 |
| **测试步骤** | 1. 在位置筛选下拉框中选择"上单" |
| **预期结果** | 1. 仅显示双方上单选手的数据（2 人）<br>2. 对称布局正常 |

| 用例编号 | MD-FILTER-004 |
|---------|--------------|
| **用例标题** | 战队筛选 + 位置筛选组合使用 |
| **模块** | 数据展示 - 筛选 |
| **优先级** | P2 |
| **前置条件** | 用户已进入对战数据详情页面 |
| **测试步骤** | 1. 战队筛选选择"战队 A"<br>2. 位置筛选选择"ADC" |
| **预期结果** | 1. 仅显示战队 A 的 ADC 选手数据（1 人） |

### 5.4 编辑模式

| 用例编号 | MD-EDIT-001 |
|---------|--------------|
| **用例标题** | 管理员进入编辑模式 |
| **模块** | 数据展示 - 编辑模式 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录，已进入对战数据详情页面（只读模式） |
| **测试步骤** | 1. 查看页面右上角<br>2. 点击"编辑"按钮 |
| **预期结果** | 1. 页面切换为编辑模式<br>2. 所有可编辑字段解锁<br>3. 显示"保存"和"取消"按钮 |

| 用例编号 | MD-EDIT-002 |
|---------|--------------|
| **用例标题** | 编辑模式下修改内容实时标记 |
| **模块** | 数据展示 - 编辑模式 |
| **优先级** | P1 |
| **前置条件** | 管理员已进入编辑模式 |
| **测试步骤** | 1. 修改某个字段（如 KDA）<br>2. 观察字段样式 |
| **预期结果** | 1. 修改过的字段边框变色，提示已修改 |

| 用例编号 | MD-EDIT-003 |
|---------|--------------|
| **用例标题** | 保存后数据生效并切换回只读模式 |
| **模块** | 数据展示 - 编辑模式 |
| **优先级** | P0 |
| **前置条件** | 管理员已在编辑模式下修改了数据 |
| **测试步骤** | 1. 点击"保存"按钮 |
| **预期结果** | 1. 保存成功，提示"保存成功"<br>2. 页面切换回只读模式<br>3. 修改的数据已生效 |

| 用例编号 | MD-EDIT-004 |
|---------|--------------|
| **用例标题** | 未保存退出时提示确认 |
| **模块** | 数据展示 - 编辑模式 |
| **优先级** | P0 |
| **前置条件** | 管理员已在编辑模式下修改了数据但未保存 |
| **测试步骤** | 1. 点击"返回"按钮或导航到其他页面 |
| **预期结果** | 1. 弹出确认对话框："有未保存的修改，是否放弃？"<br>2. 选择"取消"留在编辑页面<br>3. 选择"确定"放弃修改并退出 |

| 用例编号 | MD-EDIT-005 |
|---------|--------------|
| **用例标题** | 点击"取消"按钮放弃修改 |
| **模块** | 数据展示 - 编辑模式 |
| **优先级** | P1 |
| **前置条件** | 管理员已在编辑模式下修改了数据 |
| **测试步骤** | 1. 点击"取消"按钮 |
| **预期结果** | 1. 弹出确认对话框（如果有修改）<br>2. 确认后切换回只读模式，修改不生效 |

| 用例编号 | MD-EDIT-006 |
|---------|--------------|
| **用例标题** | 普通用户不显示"编辑"按钮 |
| **模块** | 数据展示 - 权限控制 |
| **优先级** | P0 |
| **前置条件** | 普通用户已进入对战数据详情页面 |
| **测试步骤** | 1. 查看页面右上角 |
| **预期结果** | 1. 不显示"编辑"按钮<br>2. 页面为只读模式 |

---

## 六、对局切换功能测试

### 6.1 对局切换器显示

| 用例编号 | MD-SWITCH-001 |
|---------|--------------|
| **用例标题** | BO1 赛制不显示对局切换器 |
| **模块** | 对局切换 - 显示规则 |
| **优先级** | P0 |
| **前置条件** | 用户已进入 BO1 对战数据详情页面 |
| **测试步骤** | 1. 查看页面上方对局切换区域 |
| **预期结果** | 1. 不显示对局切换器 |

| 用例编号 | MD-SWITCH-002 |
|---------|--------------|
| **用例标题** | BO3 赛制显示 3 个对局切换按钮 |
| **模块** | 对局切换 - 显示规则 |
| **优先级** | P0 |
| **前置条件** | 用户已进入 BO3 对战数据详情页面 |
| **测试步骤** | 1. 查看对局切换区域 |
| **预期结果** | 1. 显示 3 个按钮："第 1 局"、"第 2 局"、"第 3 局"<br>2. 当前局高亮显示 |

| 用例编号 | MD-SWITCH-003 |
|---------|--------------|
| **用例标题** | BO5 赛制显示 5 个对局切换按钮 |
| **模块** | 对局切换 - 显示规则 |
| **优先级** | P0 |
| **前置条件** | 用户已进入 BO5 对战数据详情页面 |
| **测试步骤** | 1. 查看对局切换区域 |
| **预期结果** | 1. 显示 5 个按钮："第 1 局"至"第 5 局"<br>2. 当前局高亮显示 |

| 用例编号 | MD-SWITCH-004 |
|---------|--------------|
| **用例标题** | 未上传数据的局显示禁用状态 |
| **模块** | 对局切换 - 显示规则 |
| **优先级** | P0 |
| **前置条件** | 用户进入 BO3 对战数据页面，仅上传了第 1、2 局数据 |
| **测试步骤** | 1. 查看第 3 局按钮 |
| **预期结果** | 1. 第 3 局按钮显示禁用状态（灰色、不可点击） |

### 6.2 对局切换交互

| 用例编号 | MD-SWITCH-005 |
|---------|--------------|
| **用例标题** | 点击对局按钮切换数据 |
| **模块** | 对局切换 - 切换交互 |
| **优先级** | P0 |
| **前置条件** | 用户进入 BO3 对战数据页面，3 局数据均已上传 |
| **测试步骤** | 1. 当前显示第 1 局<br>2. 点击"第 2 局"按钮 |
| **预期结果** | 1. 切换时显示 loading 状态（约 300ms）<br>2. 切换成功后显示第 2 局数据<br>3. "第 2 局"按钮高亮<br>4. 切换动画为淡入淡出（约 200ms） |

| 用例编号 | MD-SWITCH-006 |
|---------|--------------|
| **用例标题** | 切换后 URL 更新 |
| **模块** | 对局切换 - URL 更新 |
| **优先级** | P0 |
| **前置条件** | 用户在第 1 局页面 |
| **测试步骤** | 1. 点击"第 2 局"按钮 |
| **预期结果** | 1. URL 更新为 `/match/{id}/games?game=2` |

| 用例编号 | MD-SWITCH-007 |
|---------|--------------|
| **用例标题** | 直接通过 URL 访问指定局数 |
| **模块** | 对局切换 - URL 直接访问 |
| **优先级** | P0 |
| **前置条件** | BO3 对战，3 局数据均已上传 |
| **测试步骤** | 1. 直接在浏览器地址栏输入 `/match/{id}/games?game=3`<br>2. 回车访问 |
| **预期结果** | 1. 直接加载并显示第 3 局数据<br>2. "第 3 局"按钮高亮 |

| 用例编号 | MD-SWITCH-008 |
|---------|--------------|
| **用例标题** | 浏览器前进/后退按钮正常工作 |
| **模块** | 对局切换 - 浏览器历史 |
| **优先级** | P1 |
| **前置条件** | 用户已进行多次对局切换 |
| **测试步骤** | 1. 在第 2 局点击浏览器"后退"按钮<br>2. 再点击"前进"按钮 |
| **预期结果** | 1. 后退按钮回到第 1 局<br>2. 前进按钮回到第 2 局<br>3. 数据正确加载 |

| 用例编号 | MD-SWITCH-009 |
|---------|--------------|
| **用例标题** | 当前为第一局时数据正确加载 |
| **模块** | 对局切换 - 边界处理 |
| **优先级** | P1 |
| **前置条件** | 用户进入对战数据页面 |
| **测试步骤** | 1. 确认当前为第 1 局 |
| **预期结果** | 1. 默认加载并显示第 1 局数据 |

| 用例编号 | MD-SWITCH-010 |
|---------|--------------|
| **用例标题** | 访问不存在的局数 |
| **模块** | 对局切换 - 异常处理 |
| **优先级** | P1 |
| **前置条件** | BO3 对战 |
| **测试步骤** | 1. 访问 `/match/{id}/games?game=4` |
| **预期结果** | 1. 显示"该对局数据不存在"<br>2. 自动跳转到第一局 |

---

## 七、六芒星雷达图功能测试

### 7.1 雷达图展开/收起

| 用例编号 | MD-RADAR-001 |
|---------|--------------|
| **用例标题** | 点击选手行展开雷达图对比面板 |
| **模块** | 雷达图 - 展开/收起 |
| **优先级** | P0 |
| **前置条件** | 用户已进入对战数据详情页面 |
| **测试步骤** | 1. 点击某选手数据行（如红色方 TOP 选手） |
| **预期结果** | 1. 该选手行下方展开雷达图对比面板<br>2. 展开动画时长约 300ms<br>3. 雷达图显示该选手与对位选手（蓝色方 TOP）的 6 维度数据对比 |

| 用例编号 | MD-RADAR-002 |
|---------|--------------|
| **用例标题** | 再次点击同一选手行收起面板 |
| **模块** | 雷达图 - 展开/收起 |
| **优先级** | P1 |
| **前置条件** | 雷达图已展开 |
| **测试步骤** | 1. 再次点击同一选手行 |
| **预期结果** | 1. 雷达图收起<br>2. 收起动画时长约 300ms |

| 用例编号 | MD-RADAR-003 |
|---------|--------------|
| **用例标题** | 点击其他选手行切换对位选手对比 |
| **模块** | 雷达图 - 切换对比 |
| **优先级** | P1 |
| **前置条件** | 已展开红色方 TOP 选手的雷达图 |
| **测试步骤** | 1. 点击蓝色方 TOP 选手行 |
| **预期结果** | 1. 雷达图更新为该对位的对比数据<br>2. 切换过程平滑 |

### 7.2 雷达图维度

| 用例编号 | MD-RADAR-004 |
|---------|--------------|
| **用例标题** | 上单（TOP）选手雷达图显示正确维度 |
| **模块** | 雷达图 - 维度显示 |
| **优先级** | P0 |
| **前置条件** | 用户已进入对战数据详情页面 |
| **测试步骤** | 1. 点击 TOP 选手行展开雷达图 |
| **预期结果** | 1. 雷达图 6 个维度为：分均补刀、伤害占比、承伤占比、参团率、伤转、KDA |

| 用例编号 | MD-RADAR-005 |
|---------|--------------|
| **用例标题** | 打野（JUNGLE）选手雷达图显示正确维度 |
| **模块** | 雷达图 - 维度显示 |
| **优先级** | P0 |
| **前置条件** | 用户已进入对战数据详情页面 |
| **测试步骤** | 1. 点击 JUNGLE 选手行展开雷达图 |
| **预期结果** | 1. 雷达图 6 个维度为：分均插眼、伤害占比、承伤占比、参团率、伤转、KDA |

| 用例编号 | MD-RADAR-006 |
|---------|--------------|
| **用例标题** | 中单/ADC 选手雷达图显示正确维度 |
| **模块** | 雷达图 - 维度显示 |
| **优先级** | P0 |
| **前置条件** | 用户已进入对战数据详情页面 |
| **测试步骤** | 1. 点击 MID 选手行展开雷达图<br>2. 点击 ADC 选手行展开雷达图 |
| **预期结果** | 1. 雷达图 6 个维度为：分均补刀、伤害占比、分均经济、分均伤害、伤转、KDA |

| 用例编号 | MD-RADAR-007 |
|---------|--------------|
| **用例标题** | 辅助（SUPPORT）选手雷达图显示正确维度 |
| **模块** | 雷达图 - 维度显示 |
| **优先级** | P0 |
| **前置条件** | 用户已进入对战数据详情页面 |
| **测试步骤** | 1. 点击 SUPPORT 选手行展开雷达图 |
| **预期结果** | 1. 雷达图 6 个维度为：分均插眼、每死承伤、承伤占比、参团率、场均助攻、KDA |

| 用例编号 | MD-RADAR-008 |
|---------|--------------|
| **用例标题** | 雷达图配色正确 |
| **模块** | 雷达图 - 样式 |
| **优先级** | P1 |
| **前置条件** | 雷达图已展开 |
| **测试步骤** | 1. 观察雷达图配色 |
| **预期结果** | 1. 红色方数据区域为红色 `#f44336`（半透明填充，实线边框）<br>2. 蓝色方数据区域为青色 `#00bcd4`（半透明填充，实线边框）<br>3. 网格线为 `rgba(255, 255, 255, 0.1)` |

### 7.3 雷达图交互

| 用例编号 | MD-RADAR-009 |
|---------|--------------|
| **用例标题** | 鼠标悬停雷达图维度显示 tooltip |
| **模块** | 雷达图 - 悬停交互 |
| **优先级** | P1 |
| **前置条件** | 雷达图已展开 |
| **测试步骤** | 1. 将鼠标悬停在雷达图某个维度区域 |
| **预期结果** | 1. 显示 tooltip，包含双方该维度的具体数值 |

| 用例编号 | MD-RADAR-010 |
|---------|--------------|
| **用例标题** | 雷达图数据归一化正确 |
| **模块** | 雷达图 - 数据计算 |
| **优先级** | P1 |
| **前置条件** | 雷达图已展开 |
| **测试步骤** | 1. 检查雷达图各维度数据范围 |
| **预期结果** | 1. 每个维度的最大值为双方该维度的较大值<br>2. 最小值为 0 |

---

## 八、权限控制测试

| 用例编号 | MD-AUTH-001 |
|---------|--------------|
| **用例标题** | 管理员可访问管理页面 |
| **模块** | 权限控制 |
| **优先级** | P0 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 通过左侧菜单进入"对战数据"管理页面 |
| **预期结果** | 1. 页面正常加载<br>2. 显示已结束对战列表 |

| 用例编号 | MD-AUTH-002 |
|---------|--------------|
| **用例标题** | 普通用户访问管理页面显示无权限 |
| **模块** | 权限控制 |
| **优先级** | P0 |
| **前置条件** | 普通用户已登录 |
| **测试步骤** | 1. 直接访问管理页面 URL |
| **预期结果** | 1. 显示"无权限"提示<br>2. 不显示对战数据列表 |

| 用例编号 | MD-AUTH-003 |
|---------|--------------|
| **用例标题** | 普通用户可查看对战数据（只读模式） |
| **模块** | 权限控制 |
| **优先级** | P0 |
| **前置条件** | 普通用户已登录 |
| **测试步骤** | 1. 通过"对战数据"按钮进入对战数据页面 |
| **预期结果** | 1. 页面正常加载，显示只读模式数据<br>2. 不显示"编辑"按钮 |

| 用例编号 | MD-AUTH-004 |
|---------|--------------|
| **用例标题** | 未登录用户访问对战数据页面 |
| **模块** | 权限控制 |
| **优先级** | P0 |
| **前置条件** | 用户未登录 |
| **测试步骤** | 1. 直接访问对战数据页面 URL |
| **预期结果** | 1. 页面正常加载（数据公开可见）<br>2. 不显示任何编辑/管理按钮 |

| 用例编号 | MD-AUTH-005 |
|---------|--------------|
| **用例标题** | 未登录或 token 过期时操作返回错误码 40100 |
| **模块** | 权限控制 - API |
| **优先级** | P0 |
| **前置条件** | 用户 token 已过期 |
| **测试步骤** | 1. 执行需要鉴权的操作（如上传数据） |
| **预期结果** | 1. 返回错误码 40100<br>2. 提示"未登录或 token 过期" |

| 用例编号 | MD-AUTH-006 |
|---------|--------------|
| **用例标题** | 操作日志记录所有数据变更 |
| **模块** | 权限控制 - 审计 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 上传一份对战数据<br>2. 编辑并保存数据<br>3. 查看操作日志 |
| **预期结果** | 1. 所有变更操作均有日志记录<br>2. 日志包含操作人、操作时间、操作内容 |

---

## 九、API 接口测试

| 用例编号 | MD-API-001 |
|---------|--------------|
| **用例标题** | GET /api/matches/:matchId/games/:gameNumber - 正常获取对战数据 |
| **模块** | API 接口 |
| **优先级** | P0 |
| **前置条件** | 对战数据已存在 |
| **测试步骤** | 1. 发送 GET 请求 `/api/matches/100/games/1` |
| **预期结果** | 1. 返回状态码 200<br>2. code=0，data 包含完整的对战数据（matchId, gameNumber, winnerTeamId, gameDuration, blueTeam, redTeam, playerStats） |

| 用例编号 | MD-API-002 |
|---------|--------------|
| **用例标题** | GET /api/matches/:matchId/games/:gameNumber - 获取不存在的对战 |
| **模块** | API 接口 |
| **优先级** | P0 |
| **前置条件** | 对战 ID 99999 不存在 |
| **测试步骤** | 1. 发送 GET 请求 `/api/matches/99999/games/1` |
| **预期结果** | 1. 返回状态码 404<br>2. code=40003，message="对战数据不存在" |

| 用例编号 | MD-API-003 |
|---------|--------------|
| **用例标题** | GET /api/matches/:matchId/series - 获取对战系列信息 |
| **模块** | API 接口 |
| **优先级** | P0 |
| **前置条件** | BO3 对战已存在 |
| **测试步骤** | 1. 发送 GET 请求 `/api/matches/100/series` |
| **预期结果** | 1. 返回状态码 200<br>2. data 包含 matchId, teamA, teamB, format="BO3", games 数组（含 3 局概要信息） |

| 用例编号 | MD-API-004 |
|---------|--------------|
| **用例标题** | GET /api/matches/:matchId/games/check - 检查数据是否存在 |
| **模块** | API 接口 |
| **优先级** | P0 |
| **前置条件** | 对战 100 已上传 2 局数据 |
| **测试步骤** | 1. 发送 GET 请求 `/api/matches/100/games/check` |
| **预期结果** | 1. 返回状态码 200<br>2. data.hasData=true, data.gameCount=2 |

| 用例编号 | MD-API-005 |
|---------|--------------|
| **用例标题** | GET /api/matches/:matchId/games/check - 无数据时返回 |
| **模块** | API 接口 |
| **优先级** | P0 |
| **前置条件** | 对战 101 未上传任何数据 |
| **测试步骤** | 1. 发送 GET 请求 `/api/matches/101/games/check` |
| **预期结果** | 1. 返回状态码 200<br>2. data.hasData=false, data.gameCount=0 |

| 用例编号 | MD-API-006 |
|---------|--------------|
| **用例标题** | 统一响应格式校验 |
| **模块** | API 接口 |
| **优先级** | P1 |
| **前置条件** | 调用任意 API |
| **测试步骤** | 1. 检查所有 API 响应格式 |
| **预期结果** | 1. 所有响应包含 code、data、message 三个字段<br>2. 类型为 ApiResponse<T> |

---

## 十、边界值和异常场景测试

### 10.1 极端数值边界

| 用例编号 | MD-EDGE-001 |
|---------|--------------|
| **用例标题** | 击杀数 = 0（合法边界值） |
| **模块** | 边界值 - PlayerStats |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份所有选手击杀数均为 0 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 上传成功，0 为合法值 |

| 用例编号 | MD-EDGE-002 |
|---------|--------------|
| **用例标题** | 击杀数 = 100（合法边界值） |
| **模块** | 边界值 - PlayerStats |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份某选手击杀数为 100 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 上传成功，100 为合法边界值 |

| 用例编号 | MD-EDGE-003 |
|---------|--------------|
| **用例标题** | 等级 = 1（合法边界值） |
| **模块** | 边界值 - PlayerStats |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份某选手等级为 1 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 上传成功，1 为合法值 |

| 用例编号 | MD-EDGE-004 |
|---------|--------------|
| **用例标题** | 等级 = 18（合法边界值） |
| **模块** | 边界值 - PlayerStats |
| **优先级** | P1 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份所有选手等级均为 18 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 上传成功，18 为合法值 |

| 用例编号 | MD-EDGE-005 |
|---------|--------------|
| **用例标题** | 经济 = 0（合法边界值） |
| **模块** | 边界值 - PlayerStats |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份某选手经济为 0 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 上传成功，0 为合法值 |

| 用例编号 | MD-EDGE-006 |
|---------|--------------|
| **用例标题** | 经济 = 100000（合法边界值） |
| **模块** | 边界值 - PlayerStats |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份某选手经济为 100000 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 上传成功，100000 为合法值 |

| 用例编号 | MD-EDGE-007 |
|---------|--------------|
| **用例标题** | BO5 第 5 局数据导入 |
| **模块** | 边界值 - 局数 |
| **优先级** | P1 |
| **前置条件** | 管理员已登录，选择 BO5 赛制对战 |
| **测试步骤** | 1. 上传第 5 局数据文件（局数=5） |
| **预期结果** | 1. 上传成功<br>2. 页面可切换至第 5 局 |

### 10.2 并发与网络异常

| 用例编号 | MD-NET-001 |
|---------|--------------|
| **用例标题** | 数据加载失败自动重试 3 次 |
| **模块** | 异常场景 - 网络 |
| **优先级** | P1 |
| **前置条件** | 用户进入对战数据详情页面 |
| **测试步骤** | 1. 模拟接口前 2 次返回错误，第 3 次成功 |
| **预期结果** | 1. 系统自动重试，第 3 次成功加载数据<br>2. 用户无感知或仅短暂 loading |

| 用例编号 | MD-NET-002 |
|---------|--------------|
| **用例标题** | 重试 3 次全部失败后显示错误提示 |
| **模块** | 异常场景 - 网络 |
| **优先级** | P1 |
| **前置条件** | 用户进入对战数据详情页面 |
| **测试步骤** | 1. 模拟接口连续返回 3 次错误 |
| **预期结果** | 1. 显示"数据加载失败，请刷新重试"<br>2. 显示刷新按钮 |

| 用例编号 | MD-NET-003 |
|---------|--------------|
| **用例标题** | 网络连接异常提示 |
| **模块** | 异常场景 - 网络 |
| **优先级** | P1 |
| **前置条件** | 用户操作过程中断开网络 |
| **测试步骤** | 1. 在页面操作过程中断开网络连接<br>2. 触发数据请求 |
| **预期结果** | 1. 显示"网络连接异常，请检查网络设置"<br>2. 提供重试按钮 |

| 用例编号 | MD-NET-004 |
|---------|--------------|
| **用例标题** | 快速连续点击对局切换按钮（防抖测试） |
| **模块** | 异常场景 - 防抖 |
| **优先级** | P2 |
| **前置条件** | 用户进入 BO3 对战数据页面 |
| **测试步骤** | 1. 在 200ms 内快速连续点击"第 1 局"→"第 2 局"→"第 3 局" |
| **预期结果** | 1. 防抖生效，仅执行最后一次点击<br>2. 最终显示第 3 局数据 |

### 10.3 数据一致性

| 用例编号 | MD-CONSIST-001 |
|---------|--------------|
| **用例标题** | 选手 KDA 与战队总击杀一致性校验 |
| **模块** | 数据一致性 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份红方选手击杀总和远超战队总击杀的文件（如 5 人击杀总和=50，战队总击杀=25）<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示数据不一致警告<br>2. 或系统接受上传（需确认是否需要此校验） |

| 用例编号 | MD-CONSIST-002 |
|---------|--------------|
| **用例标题** | 获胜方战队至少有一名选手击杀数不为 0 |
| **模块** | 数据一致性 |
| **优先级** | P2 |
| **前置条件** | 管理员已登录 |
| **测试步骤** | 1. 准备一份获胜方为红方，但红方所有选手击杀数均为 0 的文件<br>2. 上传该文件 |
| **预期结果** | 1. 系统提示数据逻辑异常<br>2. 或系统接受（需确认业务规则） |

---

## 十一、响应式设计测试

### 11.1 PC 端布局

| 用例编号 | MD-RESP-001 |
|---------|--------------|
| **用例标题** | PC 端布局正常（≥1024px） |
| **模块** | 响应式设计 - PC |
| **优先级** | P1 |
| **前置条件** | 浏览器窗口宽度 ≥ 1024px |
| **测试步骤** | 1. 在 PC 端打开对战数据页面<br>2. 检查页面布局 |
| **预期结果** | 1. 多列展示，选手数据对称展示<br>2. 基本信息卡片完整显示<br>3. 对局切换按钮正常排列 |

### 11.2 平板端布局

| 用例编号 | MD-RESP-002 |
|---------|--------------|
| **用例标题** | 平板端布局正常（768px-1023px） |
| **模块** | 响应式设计 - 平板 |
| **优先级** | P1 |
| **前置条件** | 浏览器窗口宽度在 768px-1023px 之间 |
| **测试步骤** | 1. 调整浏览器窗口宽度到 900px<br>2. 检查页面布局 |
| **预期结果** | 1. 布局紧凑，选手数据行紧凑排列<br>2. 内容完整不溢出 |

### 11.3 移动端布局

| 用例编号 | MD-RESP-003 |
|---------|--------------|
| **用例标题** | 移动端布局正常（<768px） |
| **模块** | 响应式设计 - 移动端 |
| **优先级** | P1 |
| **前置条件** | 浏览器窗口宽度 < 768px |
| **测试步骤** | 1. 调整浏览器窗口宽度到 375px<br>2. 检查页面布局 |
| **预期结果** | 1. 单列布局，选手数据卡片式展示<br>2. 支持左右滑动切换选手<br>3. 对局切换器支持左右滑动<br>4. 按钮触摸尺寸 ≥ 44px |

| 用例编号 | MD-RESP-004 |
|---------|--------------|
| **用例标题** | 移动端管理页面不展示 |
| **模块** | 响应式设计 - 管理页面 |
| **优先级** | P1 |
| **前置条件** | 管理员使用移动设备访问 |
| **测试步骤** | 1. 在移动端访问管理页面 URL |
| **预期结果** | 1. 管理页面仅在 PC 端展示<br>2. 移动端可显示提示"请使用 PC 端访问管理页面" |

---

## 十二、浏览器兼容性测试

| 用例编号 | MD-COMPAT-001 |
|---------|--------------|
| **用例标题** | Chrome 90+ 兼容性 |
| **模块** | 浏览器兼容性 |
| **优先级** | P1 |
| **前置条件** | Chrome 90+ 浏览器 |
| **测试步骤** | 1. 在 Chrome 90+ 中访问对战数据页面<br>2. 执行核心操作流程（导入、展示、切换、雷达图） |
| **预期结果** | 1. 所有功能正常运行<br>2. 页面无 JS 报错<br>3. 样式正常 |

| 用例编号 | MD-COMPAT-002 |
|---------|--------------|
| **用例标题** | Firefox 88+ 兼容性 |
| **模块** | 浏览器兼容性 |
| **优先级** | P2 |
| **前置条件** | Firefox 88+ 浏览器 |
| **测试步骤** | 同上 |
| **预期结果** | 同上 |

| 用例编号 | MD-COMPAT-003 |
|---------|--------------|
| **用例标题** | Safari 14+ 兼容性 |
| **模块** | 浏览器兼容性 |
| **优先级** | P2 |
| **前置条件** | Safari 14+ 浏览器 |
| **测试步骤** | 同上 |
| **预期结果** | 同上 |

| 用例编号 | MD-COMPAT-004 |
|---------|--------------|
| **用例标题** | Edge 90+ 兼容性 |
| **模块** | 浏览器兼容性 |
| **优先级** | P2 |
| **前置条件** | Edge 90+ 浏览器 |
| **测试步骤** | 同上 |
| **预期结果** | 同上 |

---

## 十三、性能测试

| 用例编号 | MD-PERF-001 |
|---------|--------------|
| **用例标题** | 页面加载时间 < 2 秒 |
| **模块** | 性能测试 |
| **优先级** | P1 |
| **前置条件** | 网络正常 |
| **测试步骤** | 1. 使用 Lighthouse 测试对战数据页面 |
| **预期结果** | 1. 页面加载时间 < 2 秒<br>2. Lighthouse 性能得分 ≥ 90 分 |

| 用例编号 | MD-PERF-002 |
|---------|--------------|
| **用例标题** | 对局切换响应时间 < 500ms |
| **模块** | 性能测试 |
| **优先级** | P1 |
| **前置条件** | 用户进入 BO3 对战数据页面 |
| **测试步骤** | 1. 点击对局切换按钮<br>2. 使用 Performance API 测量切换耗时 |
| **预期结果** | 1. 100% 请求切换响应时间 < 500ms |

| 用例编号 | MD-PERF-003 |
|---------|--------------|
| **用例标题** | 页面 FPS ≥ 55（切换过程中） |
| **模块** | 性能测试 |
| **优先级** | P2 |
| **前置条件** | 用户进行对局切换 |
| **测试步骤** | 1. 使用 Chrome DevTools Performance 面板录制切换过程 |
| **预期结果** | 1. 切换过程中 FPS ≥ 55 |

| 用例编号 | MD-PERF-004 |
|---------|--------------|
| **用例标题** | 接口响应时间（P95）< 500ms |
| **模块** | 性能测试 |
| **优先级** | P1 |
| **前置条件** | 后端服务正常 |
| **测试步骤** | 1. 使用性能测试工具对战数据接口进行压测 |
| **预期结果** | 1. 接口 P95 响应时间 < 500ms |

---

## 十四、数据埋点测试

| 用例编号 | MD-TRACK-001 |
|---------|--------------|
| **用例标题** | 对战数据页面访问埋点（match_data_page_view） |
| **模块** | 数据埋点 |
| **优先级** | P1 |
| **前置条件** | 页面已部署埋点 |
| **测试步骤** | 1. 进入对战数据页面<br>2. 检查埋点上报数据 |
| **预期结果** | 1. 上报事件 match_data_page_view<br>2. 属性包含 match_id、game_number、format |

| 用例编号 | MD-TRACK-002 |
|---------|--------------|
| **用例标题** | 对局切换埋点（match_game_switch） |
| **模块** | 数据埋点 |
| **优先级** | P1 |
| **前置条件** | 页面已部署埋点 |
| **测试步骤** | 1. 执行对局切换操作<br>2. 检查埋点上报数据 |
| **预期结果** | 1. 上报事件 match_game_switch<br>2. 属性包含 match_id、from_game、to_game、method |

| 用例编号 | MD-TRACK-003 |
|---------|--------------|
| **用例标题** | 选手雷达图展开埋点（match_player_radar_expand） |
| **模块** | 数据埋点 |
| **优先级** | P1 |
| **前置条件** | 页面已部署埋点 |
| **测试步骤** | 1. 点击选手行展开雷达图<br>2. 检查埋点上报数据 |
| **预期结果** | 1. 上报事件 match_player_radar_expand<br>2. 属性包含 match_id、game_number、player_id、opponent_id、role |

| 用例编号 | MD-TRACK-004 |
|---------|--------------|
| **用例标题** | 埋点数据 100% 全量上报 |
| **模块** | 数据埋点 |
| **优先级** | P1 |
| **前置条件** | 页面已部署埋点 |
| **测试步骤** | 1. 执行所有触发埋点的操作<br>2. 检查数据分析平台 |
| **预期结果** | 1. 所有事件均有记录<br>2. 采样率为 100% |

---

## 测试用例统计

| 模块 | 用例数量 | P0 | P1 | P2 |
|------|---------|----|----|----|
| 一、Excel 文件导入功能 | 28 | 14 | 11 | 3 |
| 二、数据解析功能 | 5 | 3 | 2 | 0 |
| 三、数据关联功能 | 8 | 4 | 4 | 0 |
| 四、管理页面功能 | 8 | 4 | 4 | 0 |
| 五、数据展示功能 | 16 | 7 | 7 | 2 |
| 六、对局切换功能 | 10 | 5 | 4 | 1 |
| 七、六芒星雷达图功能 | 10 | 4 | 6 | 0 |
| 八、权限控制 | 6 | 5 | 0 | 1 |
| 九、API 接口测试 | 6 | 5 | 1 | 0 |
| 十、边界值和异常场景 | 11 | 0 | 7 | 4 |
| 十一、响应式设计 | 4 | 0 | 4 | 0 |
| 十二、浏览器兼容性 | 4 | 0 | 1 | 3 |
| 十三、性能测试 | 4 | 0 | 3 | 1 |
| 十四、数据埋点 | 4 | 0 | 4 | 0 |
| **合计** | **119** | **51** | **58** | **10** |

---

## 测试用例执行优先级建议

1. **第一轮（P0 用例，共 51 个）**: 核心功能验证，包括文件导入、数据解析、数据关联、页面展示、对局切换、权限控制、API 接口
2. **第二轮（P1 用例，共 58 个）**: 异常场景和边界值覆盖，包括格式校验、数值校验、筛选功能、编辑模式、网络异常、响应式、性能
3. **第三轮（P2 用例，共 10 个）**: 边缘场景和用户体验优化，包括浏览器兼容性、极端数值、操作日志

---

## 高风险功能回归重点

| 功能 | 风险等级 | 回归重点 |
|------|---------|---------|
| Excel 导入 | 高 | 文件格式校验、数据校验、关联匹配准确性 |
| 权限控制 | 高 | 普通用户不可编辑、管理员操作日志 |
| 对局切换 | 中 | URL 更新、浏览器历史、未上传数据禁用 |
| 雷达图 | 中 | 各位置维度正确性、配色、交互流畅度 |
| 数据一致性 | 低 | KDA 与战队统计一致性校验 |
