{
  "name": "lvjiang-cup-test-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "test": "npm run test --workspace=backend && npm run test --workspace=frontend",
    "test:e2e": "npm run test:e2e --workspace=frontend",
    "test:integration": "npm run test:integration --workspace=backend",
    "test:all": "npm run test --workspace=backend && npm run test --workspace=frontend && npm run test:integration --workspace=backend"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "better-sqlite3": "^12.10.0"
  }
}
